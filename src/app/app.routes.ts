import { Routes } from '@angular/router';
import { ModifyComponent } from './Contact/modify/modify.component';

export const routes: Routes = [
  //{ path: 'new', component: ModifyComponent },
  //{ path: 'edit/:id', component: ModifyComponent },
];
